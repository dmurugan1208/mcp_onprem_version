{
  "name": "osfi_search_guidance",
  "implementation": "sajha.tools.impl.osfi_tools.OsfiSearchGuidanceTool",
  "description": "Search across all local OSFI guideline documents for a keyword or topic. Returns matching section headings and excerpts with character offsets. Use this before osfi_read_document to find exactly where to read.",
  "version": "1.0.0",
  "enabled": true,
  "data_path": "${data.osfi_docs_dir}",
  "inputSchema": {
    "type": "object",
    "properties": {
      "keyword": {"type": "string", "description": "Search term — case-insensitive partial match"},
      "doc_filter": {"type": "string", "description": "Limit to specific doc or guideline code e.g. CAR or B-13"},
      "max_results": {"type": "integer", "default": 5}
    },
    "required": ["keyword"]
  },
  "metadata": {
    "author": "Ashutosh Sinha",
    "email": "ajsinha@gmail.com",
    "category": "Regulatory Intelligence",
    "tags": ["osfi", "bsif", "regulatory", "canadian banking", "guidelines"],
    "rateLimit": 30,
    "cacheTTL": 300,
    "copyright": "Copyright All Rights Reserved 2025-2030"
  }
}
